{
  "name": "app",
  "version": "1.0.0",
  "main": "main/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=development concurrently \"cd renderer && npm run dev\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "cd renderer && npm run build && electron-builder"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cross-env": "^10.1.0",
    "electron": "^38.2.1"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "electron-builder": "^26.0.12",
    "electron-reload": "^2.0.0-alpha.1",
    "wait-on": "^9.0.1"
  }
}
